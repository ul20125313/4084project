<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dazuoye">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="Flavor"
        android:name=".DzyApplication"
        android:roundIcon="@mipmap/icon"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_key" />
        <activity android:name=".userpage.wo.touxiang"></activity>
        <activity android:name=".logpage.zcym" />
        <activity android:name=".userpage.dingdan.chakandingdan" />
        <activity android:name=".userpage.caidan.ddxq" />
        <activity android:name=".userpage.wo.xiugai" />
        <activity android:name=".userpage.mainpage" />
        <activity android:name=".logpage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>